- name: reload systemd 
  become: yes
  ansible.builtin.systemd:
    daemon_reload: yes

- name: restart IPFS Cluster
  become: yes
  ansible.builtin.service:
    name: ipfs-cluster
    enabled: yes
    state: restarted